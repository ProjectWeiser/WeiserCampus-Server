'use strict';

module.exports = {
  /**
   * Processes data from LiveViewGPS for WeiserCampus clients.
   * 
   * @param {Object} data - Response from LiveViewGPS.
   * @returns {Object} - Formatted response.
   */
  process(data) {
    return data;
  },
};
